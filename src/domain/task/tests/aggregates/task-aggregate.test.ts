import { TaskAggregate } from "../../aggregate/task-aggregates";

describe("Task Aggregate", () => {
  test(`WHEN create new task,
  THEN it should return the create task value`, () => {
    expect(true).toBe(true);
  });
});
